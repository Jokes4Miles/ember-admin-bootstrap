<div class="ember-admin container-fluid">
  <div class="row">
    <div class="col-xs-12">
      <nav class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            {{link-to 'Admin' 'admin' class='navbar-brand'}}
            <button class='navbar-toggle collapsed' type='button' data-toggle='collapse' data-target='#model-navbar' aria-expanded='false'>
              <span class='sr-only'></span>
              <span class='icon-bar'></span>
              <span class='icon-bar'></span>
              <span class='icon-bar'></span>
            </button>
          </div>

          <div id='model-navbar' class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              {{#each filteredModels as |name|}}
                <li>{{link-to name 'model-records' name}}</li>
              {{/each}}
            </ul>
          </div>
        </div>
      </nav>
    </div>
  </div>

  <div class="row">
    {{outlet}}
  </div>
</div>
